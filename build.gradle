buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath(libs.agp)
        classpath(libs.kgp)
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}